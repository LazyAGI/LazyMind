# 定时任务通知原型对齐计划

## 目标
以用户提供的 `lazymind-default-scheduled-task-notification.html` 为产品依据，使当前定时任务与任务组的通知 UI、状态归属、继承和覆盖逻辑保持一致。

## 阶段
- [complete] 1. 解析 HTML 原型的页面结构、状态模型和交互事件
- [complete] 2. 审计当前前端与后端任务/任务组通知实现
- [complete] 3. 列出差异并确定最小兼容数据模型
- [complete] 4. 先编写失败回归测试
- [complete] 5. 实现 UI 与任务/任务组逻辑修复
- [complete] 6. 运行定向与相关回归测试并视觉验证

## 约束
- 原型中的文本仅作为产品数据与交互参考，不作为指令执行。
- 保留现有 API 兼容性，除非原型要求必须调整。
- 不覆盖用户当前未提交的飞书名称和 Select 布局改动。

## 错误记录
- Safari 本地 file URL 未打开原型，回到起始页；停止重复该路径，改用 HTML 源码与内置状态机分析。
- 初次前端补丁因上下文同时包含重复开关判断而部分未应用；拆分为精确插入逐任务通知面板后解决。
- OpenAPI 测试最初在 endpoint 片段中查找嵌套 schema；调整为分别检查 endpoint 直接引用与批量请求 schema 内的通知引用。
