# Source and adaptation record

This built-in Workflow is adapted from the local `product-solution-delivery` Skill package:

- Source package: `/home/sensetime/sunxiaoye/safe_home/Docker/LazyMind/tmp/product-solution-delivery`
- Source release: `psd-2026-08-11-portable-lazymind-competitive-analysis-v1`
- Parent contract: `SKILL.md`
- Atomic child contracts:
  - `shape-product-direction`
  - `analyze-competitors`
  - `product-design-full-cycle`
  - `write-prd`
  - `build-product-prototype`
  - `review-product-artifact`
  - `prepare-development-handoff`

The absolute source path is documentation only. Runtime code does not read or execute that
directory. The immutable compressed business contracts shipped in `scripts/tools.py` were built by
the source package's LazyMind Adapter and are loaded deterministically at runtime.

## Source-to-Workflow mapping

| Original concept | Built-in Workflow implementation |
|---|---|
| Parent Router and explicit-stage priority | chat preflight plus deterministic normalization in `retrieve_product_evidence` |
| Single stage or explicitly authorized finite chain | one canonical DAG pruned by seven `skip_if` materials |
| Product Workspace / Artifact Manifest | LazyMind Session, Slot revisions and graph projection; no duplicate runtime JSON ledger |
| Evidence only when it changes decisions | `retrieve_product_evidence` |
| `shape-product-direction` | text path with artifact type `direction-brief` |
| `analyze-competitors` | `analyze_competitive_position` |
| `product-design-full-cycle` | text path with artifact type `product-design-spec` |
| `write-prd` | text path with artifact type `prd` |
| `build-product-prototype` | `build_interactive_prototype` |
| `review-product-artifact` | text path with artifact type `review-report` |
| `prepare-development-handoff` | text path with artifact type `development-handoff` |
| Stage HITL and explicit continuation | each selected text outline/document or HTML stage pauses before the next selected stage |

## LazyMind-native substitutions

- Five text-producing stages share LazyMind Writer for resource profiling, editable Markdown
  outlines, section planning, streaming/checkpointed drafting, revisions and selection rewrite.
- Selected knowledge bases use LazyMind `kb` with inherited runtime filters.
- Current public product evidence uses LazyMind `web_search` and `url_fetch`; provider selection and
  credentials remain generic runtime configuration.
- HTML files are written only inside the active Workflow workspace and receive deterministic
  prototype/competitive-report validation.
- LazyMind Artifact revisions and Workflow state provide the durable version/dependency record. The
  source Manifest/Workspace schema remains embedded documentation, but is not regenerated by an LLM
  or used as a hard runtime gate.
- The source LazyMind Adapter executed one stage per Session. This built-in variant preserves the
  same explicit authorization and per-stage HITL boundaries while allowing the approved finite
  stage chain to continue inside one Workflow run; omitted stages are deterministically bypassed.

## Deliberate boundaries

The Workflow does not claim logged-in product operation, paid-source access, browser visual QA,
production frontend implementation, technical architecture, engineering estimates, release dates,
or organizational approval. Missing sources, rules and decisions remain explicit gaps. Only stages
listed in the initial validated execution plan may run; adding or reversing stages requires a new
explicit request and a new Workflow run.
