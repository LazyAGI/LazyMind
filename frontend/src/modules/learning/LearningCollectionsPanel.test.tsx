import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { vi } from "vitest";
import LearningCollectionsPanel from "./LearningCollectionsPanel";
import * as api from "./api";
vi.mock("./api",()=>({getLearningCatalog:vi.fn(),listLearningBooks:vi.fn(),createLearningBook:vi.fn(),createLearningReviewSession:vi.fn(),answerLearningQuestion:vi.fn(),importLearningDictionary:vi.fn()}));
const catalog={capabilities:[{key:"classical_translation",version:1,name_i18n_key:"文言翻译",description_i18n_key:"",local_only:true,languages:["lzh"],subject_kinds:["sentence"],fields:[],provider_pipeline:[],allowed_question_types:["rubric_self_assessment"],default_question_types:["rubric_self_assessment"]}],question_types:[{key:"rubric_self_assessment",version:1,name_i18n_key:"自评题",dynamic:true}],profiles:[],local_available:true};
it("renders a dynamic session snapshot and submits self assessment",async()=>{vi.mocked(api.getLearningCatalog).mockResolvedValue(catalog);vi.mocked(api.listLearningBooks).mockResolvedValue([{id:"b",name:"翻译集",description:"",capability_key:"classical_translation",question_types_json:'["rubric_self_assessment"]'}]);vi.mocked(api.createLearningReviewSession).mockResolvedValue({session:{id:"s",total:1,answered:0,status:"active"},items:[{question:{id:"q",question_type:"rubric_self_assessment",payload_json:'{"prompt":"翻译此句"}'},card:{id:"c",question_type:"rubric_self_assessment"}}]});vi.mocked(api.answerLearningQuestion).mockResolvedValue({});render(<LearningCollectionsPanel localAvailable/>);fireEvent.click(await screen.findByRole("button",{name:"learning.startReview"}));expect(await screen.findByText("翻译此句")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"learning.rating.good"}));await waitFor(()=>expect(api.answerLearningQuestion).toHaveBeenCalledWith("s",expect.objectContaining({question_id:"q",rating:"good"})));});
