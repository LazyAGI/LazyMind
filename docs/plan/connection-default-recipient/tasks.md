# 任务

1. 复用官方 Python SDK 增加分页群发现、群名称和成员状态校验。
2. 账号默认对象及目标元数据双库增量持久化，防止归属错误及凭据变更竞态。
3. 增加集中授权的默认对象/群候选接口，群发送前验证，保留原有通知对象接口兼容。
4. 同步手工 OpenAPI、生成客户端、API 文档、中英文提示。
5. 复用接收对象选择器实现连接默认编辑及任务默认带入，扫码成功后提供设置入口。
6. 聚焦后端双库、SDK边界和前端回归，确保旧任务不随默认变化改绑。
7. 构建与桌面接口/实际界面验收；需要新的平台权限时明确实测限制。
