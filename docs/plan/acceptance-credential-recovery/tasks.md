# 工作清单

1. **定位与只读核对（完成）**：确认独立执行也失败、当前只有一条模型密文、旧密钥可读而桌面密钥不可读；不输出凭据。
2. **恢复测试（完成）**：`backend/core/cmd/acceptance-model-credentials/recovery_test.go`，使用临时文件 SQLite 和合成凭据覆盖预检、恢复、备份、幂等、回滚及安全拒绝。依赖既有 `common/secretcrypto`。
3. **人工 Review（已批准）**：用户回复“确认”批准测试与方案；阶段一未提前实现生产行为，阶段二完成恢复。
4. **实现离线维护命令（完成）**：同目录新增入口、完成恢复函数；复用既有 AES-GCM 实现，严格解析路径/身份/状态，密钥不进参数或输出，默认 dry-run。
5. **备份与失败恢复（完成）**：验证事务失败及配置写入失败；SQLite 一致性备份，配置安全替换，处理重复执行和中断后恢复，不覆盖未知配置。
6. **当前验收环境执行（完成）**：确认无活动任务、停止服务，先预检，再备份并 apply；操作时使用现有设备身份，无需修改/重建桌面包。
7. **主流程验证与交付（完成）**：桌面任务执行成功，Local 同库执行成功，再切回桌面；记录实际结果、受保护备份位置及恢复方法。相关测试、gofmt、diff 检查通过，不扩展无关模块。
