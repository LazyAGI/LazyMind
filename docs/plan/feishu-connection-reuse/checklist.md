# 验收清单

- [x] 普通入口已有账号时不自动调用强制创建注册；无账号时首次创建一次。
- [x] 多账号由用户明确选择，同名账号不会自动合并或选错。
- [x] 复用已连接账号时 account_id/app_id 均不改变，通知规则及历史记录保留。
- [x] 暂停保留密钥且禁止收发；有效凭据恢复免授权；解除绑定清密钥，已撤销凭据不得复活。
- [x] 重连返回不同身份时维持 ACCOUNT_IDENTITY_MISMATCH；不替换原账号、不挂错通知规则。
- [x] 跨用户目标返回既有所有权错误，不读取或返回其他用户密钥。
- [x] 明确新增机器人方可调用强制创建；旧客户端不会隐式新增重复应用。
- [x] 同一幂等键与并发同一意图只触发一次注册；不同意图/目标冲突安全拒绝。
- [x] 取消、过期及中断恢复不把复用改成创建，不复活已断开的通知投递。
- [x] 前端明确展示复用/重新授权/新增，敏感凭据不进源码、日志或响应。
- [x] 文件 SQLite 和 PostgreSQL 的真实数据库测试分别通过；API/错误文档与实现一致。
- [x] 微信、企微与飞书既有对话/通知回归通过。
- [ ] 用户完成一次真实“已连接→复用”和“暂停→免授权恢复，以及解除绑定→重新授权原应用”验收，飞书侧 app_id 不变、本地 account_id 不变。
- [ ] 显式新增独立机器人经用户实际授权验收；不删除既有重复机器人。

当前状态：用户要求直接实现并最后测试后，已完成代码、双数据库及前端自动化验证；勾选项表示自动验证通过，真实平台人工验收两项待完成。详见 delivery.md。


## 账号管理补充（2026-09-18）

用户再次要求直接实现、最后测试。按 account-management-proposal.md 实现解绑记录软删除、备注编辑、授权人和应用标识展示及选择器一致性。新增两个增量字段，按双数据库验证升级、数据保留、重复迁移与归档后不可重连；不物理删除历史或飞书应用。

- [x] 飞书备注、授权人、应用 ID 和应用内用户 ID 展示，配置选择器标识一致。
- [x] 仅已解绑记录可归档；引用查询失败时不进入删除确认。
- [x] 删除幂等、所有权拒绝、历史保留、不误删同名账号、重启仍隐藏。
- [x] 归档与授权并发、迟到回调无法恢复原记录。
- [x] 新旧文件 SQLite/PostgreSQL 初始化、升级和重复初始化均保留数据。
- [x] 统一测试通过，桌面重建启动，真实受保护接口验证通过。
- [x] 解锁后完成实际画面、备注保存/刷新/恢复、空值校验及删除确认取消验收，结果见 account-management-delivery.md。
- [x] 修正保存按钮和账号列表标题两处文案；58 项前端测试、ESLint、构建通过，标题已桌面实测。当前无活动账号，保存弹窗的再次桌面实测未执行。
- [ ] 真实归档提交、非零引用提示及有效飞书账号重新授权/免授权重连的平台实测仍待完成。
