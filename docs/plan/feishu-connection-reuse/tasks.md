# 实施任务

1. 核对 SDK 现有应用授权能力、注册回调和当前账号元数据；源码依据：feishu/registration.py、feishu/connection.py、feishu/accounts.py、common/infrastructure/postgres.py 与 sqlite.py。记录真实授权页面尚待验证。
2. 方案已按用户确认更新，补齐 API 意图字段、稳定错误及兼容策略；旧客户端默认不得继续隐式创建重复应用。
3. 阶段一新增测试：现有连接复用、首次创建、显式新增、暂停免授权恢复及解除绑定后重新授权、身份不匹配、跨用户拒绝、取消/过期、幂等与并发；检查注册调用次数及应用/account_id 不变。
4. 阶段一双数据库集成测试：文件 SQLite 与 PostgreSQL 分别验证会话互斥、事务及历史/通知引用保留；不以 Mock 或 SQLite 代替 PostgreSQL。
5. 阶段一前端测试：已有连接选择、独立新增入口、重连说明、同名多个账号不误选、微信/企微回归。测试位置遵循现有通知目录和渠道测试布局。
6. 已提交测试报告；用户后续明确要求直接实现、最后统一测试，本批按新指令继续。保留原 34 项测试，补充实施发现的边界场景。
7. 阶段二复用现有会话/注册适配器实现显式意图和后端强制校验，同步接口契约与错误文案；最小修改渠道页面和中英文文案。
8. 运行受影响 pytest、flake8、前端测试/lint/build及 SQLite/PostgreSQL 回归，记录阻塞与未验证项。
9. 接入主流程：构建桌面版、复用现有验收环境，用户完成真实扫码验证同一机器人复用及显式新增；既有连接、通知引用和其它渠道不受影响。记录交付结果，不自行删除已有机器人。


## 账号管理补充（2026-09-18）

用户再次要求直接实现、最后测试。按 account-management-proposal.md 实现解绑记录软删除、备注编辑、授权人和应用标识展示及选择器一致性。新增两个增量字段，按双数据库验证升级、数据保留、重复迁移与归档后不可重连；不物理删除历史或飞书应用。
