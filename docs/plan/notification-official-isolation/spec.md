# 通知功能官方基线隔离

用户于 2026-09-17 要求：修正通知功能依附工作区功能分支的问题，修正结束后删除原通知分支。

## 目标与边界

在官方 LazyAGI/LazyMind main `35ea71810d17f622febc1c04caa8c1d9ec9cbb03` 上保留此前批准的微信/iLink、企业微信智能机器人、飞书、定时任务通知、原生桌面通知、凭证续期和 HTML 对应前端。业务要求沿用原功能 spec 及审查修复方案；不重新定义 API、默认行为、身份边界和通知语义。

只迁移 `ef3258d5..146e4bae` 中通知相关差异及后续审查/前端/启动修复，剔除工作区审批测试修改、重新授权修复及其专用测试。保留官方近期新增浏览器、文档学习、外部能力等代码；不复制整份旧文件覆盖官方。

旧目录所有未提交文件与 Git 历史在外部备份。旧通知分支仅在新代码验证与交付完成后删除；不删除 feature/newWorkZone 或另一 Feishu 分支，不清空用户运行数据。

## 完成标准

- 新功能提交直接基于上述官方提交，历史不包含 feature/newWorkZone 的独有提交。
- 新功能差异不存在工作区授权生产代码、路由或迁移。
- 相关 Core/Gateway 两数据库、Desktop、CLI、运行管理器、前端测试和构建验证完成；官方基线失败分别记录，不把既有失败当作新功能通过。
- 新分支上传用户 fork 后再删除原通知分支，原始修改可由 bundle、patch 与 untracked 归档恢复。
- 桌面包以新源码重建，旧包不作为本次隔离交付。
